/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['linkpicture.com','img.icons8.com', 'media.licdn.com', 'miro.medium.com']
  }
}

module.exports = nextConfig
