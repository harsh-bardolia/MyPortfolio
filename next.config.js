/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['linkpicture.com','freepnglogos.com','img.icons8.com']
  }
}

module.exports = nextConfig
